<div class="card-body">
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-dark w-100 d-block mt-3">{{ button_label|default('Enregistrer') }}</button>
	{{ form_end(form) }}
</div>
