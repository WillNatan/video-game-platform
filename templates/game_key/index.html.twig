{% extends 'base.html.twig' %}

{% block title %}GameKey index{% endblock %}

{% block body %}
    <h1 class="titreindex">GameKey index</h1>

    <div class="container">
    <div class="card text-left shadow-lg">
      <img class="card-img-top" src="holder.js/100px180/" alt="">
      <div class="card-body">
        <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Key</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for game_key in game_keys %}
            <tr>
                <td>{{ game_key.id }}</td>
                <td>{{ game_key.key }}</td>
                <td>
                    <a href="{{ path('game_key_show', {'id': game_key.id}) }}">show</a>
                    <a href="{{ path('game_key_edit', {'id': game_key.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
        </table>
        <button onclick="window.location.href ='{{ path('game_key_new') }}'"type="button" class="btn btn-dark">
            Ajouter une clée
        </button>
      </div>
    </div>
    </div>
{% endblock %}

