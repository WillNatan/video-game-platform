{% extends 'base.html.twig' %}

{% block title %}Modification clé cd
{% endblock %}

{% block body %}
	<div class="container">
		<div class="card">
			<div class="card-header">
				<h3 class="titreindex">Modification clé cd</h3>
			</div>
			{{ include('game_key/_form.html.twig', {'button_label': 'Modifier'}) }}
			<div class="card-footer">
				<div class="d-flex justify-content-center flex-wrap">
					<a href="{{ path('game_key_index') }}" class="btn btn-dark"><i class="fa fa-arrow-left"></i></a>

					{{ include('game_key/_delete_form.html.twig') }}
				</div>
			</div>
		</div>
	</div>


{% endblock %}
