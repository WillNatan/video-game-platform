{% extends 'base.html.twig' %}

{% block title %}Edit GameKey
{% endblock %}

{% block body %}
	<h1 class="titreindex">Edit GameKey</h1>
	<div class="container w-50">

		{{ include('game_key/_form.html.twig', {'button_label': 'Update'}) }}

		<div class="d-flex">
			<a href="{{ path('game_key_index') }}" class="btn btn-dark mt-3">back to list</a>

			{{ include('game_key/_delete_form.html.twig') }}
		</div>
	</div>


{% endblock %}
