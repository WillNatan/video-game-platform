<div class="card-body">
	<div class="form-group">
		{{ form_start(form) }}
		{{ form_widget(form) }}
	</div>
	<button class="btn btn-dark w-100 d-block mt-3">{{ button_label|default('Ajouter') }}</button>
	{{ form_end(form) }}
</div>
