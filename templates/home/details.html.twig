{% extends 'frontbase.html.twig' %}

{% block title %}ActuGames - Accueil
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6">
            <img class="d-block w-100" src="{{ asset('img/gameimg/nfs.png') }}" alt="Third slide">
        </div>
        <div class="col-md-6">
            <h1 class="text-light">{{ game.name }}</h1>
            <p>{{ game.description }}</p>
            <h1 class="text-success">{{ game.price }} €</h1>
            <a href="" class="btn btn-success">Ajouter au panier</a>
        </div>
    </div>
{% endblock %}