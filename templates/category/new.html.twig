{% extends 'base.html.twig' %}

{% block title %}Ajouter une catégorie
{% endblock %}

{% block body %}
	<div class="container">
		<div class="card">
			<div class="card-header">
				<h3 class="titreindex">Ajouter une catégorie</h3>
			</div>
			{{ include('category/_form.html.twig') }}
			<div class="card-footer">
				<a href="{{ path('category_index') }}" class="btn btn-dark">
					<i class="fa fa-arrow-left"></i>
				</a>
			</div>
		</div>
	</div>


{% endblock %}
