{% extends 'base.html.twig' %}

{% block title %}Category
{% endblock %}

{% block body %}
	<h1 class="titreindex">Category</h1>
	.<div class="card text-left shadow-lg">
		<img class="card-img-top" src="holder.js/100px180/" alt="">
		<div class="card-body">
			<table class="table">
				<tbody>
					<tr>
						<th>Id</th>
						<td>{{ category.id }}</td>
					</tr>
					<tr>
						<th>Name</th>
						<td>{{ category.name }}</td>
					</tr>
				</tbody>
			</table>
		</div>
	</div>

<div class="d-flex mt-3">
	<a href="{{ path('category_index') }}" class="btn btn-dark">back to list</a>

	<a href="{{ path('category_edit', {'id': category.id}) }}" class="btn btn-dark ml-3 " >edit</a>

	{{ include('category/_delete_form.html.twig') }}
</div>
{% endblock %}
